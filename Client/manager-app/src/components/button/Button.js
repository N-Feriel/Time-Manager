import styled from 'styled-components';

const Button = styled.button`
    position: relative;
    display: block;
    border-radius: 12px;
    /* color: white; */
    border: none;
    padding: 10px;
    margin: 10px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
`;

export default Button;